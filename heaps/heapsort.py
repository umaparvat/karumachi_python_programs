"""
create a max heap tree
swap root with last
reduce heap size
heapify the remaining elements in heap tree
repeat until it reduces to single element in tree
now the heap will have elements in ascending order

To create descending order
1. create min heap
2. swap root with last
3. reduce size
4. heapify until single element reaches
5. now heap tree will have elements in descending order.
"""